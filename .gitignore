# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/

# Env / secrets
.env
.env.local

# Node / Next.js (added at L4)
node_modules/
.next/
out/

# Local data / DB
*.db
*.sqlite3
backend/test_policy.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Provided assignment briefs & data ARE committed (alongside our README.md and
# ASSUMPTIONS.md) so reviewers can see the rules the engine implements and so the
# eval harness + acceptance tests run on a fresh clone. See README "Testing".
