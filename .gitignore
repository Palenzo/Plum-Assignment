# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Env / secrets
.env
.env.local

# Node / Next.js (added at L4)
node_modules/
.next/
out/

# Local data / DB
*.db
*.sqlite3

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Provided assignment briefs (inputs — not part of the submission repo)
# NOTE: ASSUMPTIONS.md is intentionally NOT ignored — it is a deliverable.
README.md
plum_intern_assignment.md
adjudication_rules.md
sample_documents_guide.md

# Provided data files (root only — the app keeps its own copy under backend/app/data)
/test_cases.json
/policy_terms.json
